version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign

formatters:
  enable:
    - gofmt

linters-settings:
  errcheck:
    check-type-assertions: true

issues:
  exclude-dirs:
    - internal/database/sqlc
